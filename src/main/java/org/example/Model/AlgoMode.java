package org.example.Model;

/**
 * The different modes the AI can be in. HUNT mode is when the AI is looking for a ship, TARGET mode
 * is when the AI has found a ship and is trying to sink it.
 */
public enum AlgoMode {
    HUNT,
    TARGET
}
